<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Workouts - <%= appData.appName %></title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>
<body>
    <div class="container">
        <header>
            <h1>My Workouts</h1>
        </header>

        <nav class="breadcrumb">
            <a href="/">Home</a> &gt; <a href="/dashboard">Dashboard</a> &gt; My Workouts
        </nav>

        <main class="content">
            <div class="action-bar">
                <a href="/workouts/add" class="btn btn-primary">Add New Workout</a>
                <a href="/workouts/search" class="btn btn-secondary">Search Workouts</a>
            </div>

            <% if (workouts.length === 0) { %>
                <p class="no-data">No workouts recorded yet. Start by adding your first workout!</p>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Name</th>
                            <th>Type</th>
                            <th>Duration (min)</th>
                            <th>Calories</th>
                            <th>Distance (km)</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% workouts.forEach(function(workout) { %>
                            <tr>
                                <td><%= new Date(workout.workout_date).toLocaleDateString() %></td>
                                <td><%= workout.name %></td>
                                <td><span class="badge badge-<%= workout.workout_type.toLowerCase() %>"><%= workout.workout_type %></span></td>
                                <td><%= workout.duration_minutes || '-' %></td>
                                <td><%= workout.calories_burned || '-' %></td>
                                <td><%= workout.distance_km ? parseFloat(workout.distance_km).toFixed(2) : '-' %></td>
                                <td><a href="/workouts/<%= workout.id %>" class="btn btn-small">View</a></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } %>
        </main>

        <footer>
            <p><a href="/">Home</a> | <a href="/dashboard">Dashboard</a> | <a href="/logout">Logout</a></p>
            <p>&copy; 2024 <%= appData.appName %></p>
        </footer>
    </div>
</body>
</html>
